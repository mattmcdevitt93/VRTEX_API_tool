<h1>Listing Timesheets</h1>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Event time</th>
      <th>User</th>
      <th>Urgency</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @timesheets.each do |timesheet| %>
      <tr>
        <td><%= timesheet.event %></td>
        <td><%= timesheet.event_time %></td>
        <td><%= timesheet.user_id %></td>
        <td><%= timesheet.urgency %></td>
        <td><%= timesheet.notes %></td>
        <td><%= link_to 'Show', timesheet %></td>
        <td><%= link_to 'Edit', edit_timesheet_path(timesheet) %></td>
        <td><%= link_to 'Destroy', timesheet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Timesheet', new_timesheet_path %>
