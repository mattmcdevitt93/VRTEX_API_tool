Dashboard Page
<div>
	<% if current_user.blank? === false %>
		<% if current_user.valid_api == true %>
		Change Primary Character
		<% else %>
		Pending validation or error
		<% end %>
	<% end %>

</div>